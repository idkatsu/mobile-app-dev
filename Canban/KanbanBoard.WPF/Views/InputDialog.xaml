<Window x:Class="KanbanBoard.WPF.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод" Width="350" Height="160"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="PromptText" FontSize="14" Foreground="{DynamicResource AppForeground}"/>
        <TextBox Grid.Row="2" x:Name="InputBox" Height="30" FontSize="14"
                 Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource AppForeground}"
                 BorderBrush="{DynamicResource BorderColor}" KeyDown="InputBox_KeyDown"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="Ok_Click" Padding="16,6" Margin="0,0,8,0"
                    Background="{DynamicResource AccentColor}" Foreground="{DynamicResource AccentForeground}" FontWeight="SemiBold"/>
            <Button Content="Отмена" Click="Cancel_Click" Padding="16,6"
                    Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}"/>
        </StackPanel>
    </Grid>
</Window>
