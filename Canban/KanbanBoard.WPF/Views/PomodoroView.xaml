<UserControl x:Class="KanbanBoard.WPF.Views.PomodoroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackground}">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,30">
        <StackPanel HorizontalAlignment="Center" Width="320">
            <TextBlock Text="{Binding PomodoroModeText}" FontSize="18" FontWeight="SemiBold"
                       Foreground="{DynamicResource SidebarForeground}" HorizontalAlignment="Center" Margin="0,0,0,16"/>

            <Canvas Width="260" Height="260" HorizontalAlignment="Center" Margin="0,0,0,16">
                <Ellipse Width="250" Height="250" Stroke="{DynamicResource BorderColor}" StrokeThickness="6"
                         Fill="Transparent" Canvas.Left="5" Canvas.Top="5"/>
                <Path x:Name="TimerArc" Stroke="{DynamicResource AccentColor}" StrokeThickness="8"
                      Canvas.Left="5" Canvas.Top="5"/>
                <TextBlock x:Name="TimerText" Text="{Binding TimerDisplay}" FontSize="42" FontWeight="Bold"
                           Foreground="{DynamicResource AppForeground}"
                           Canvas.Left="80" Canvas.Top="95"/>
            </Canvas>

            <TextBlock Text="{Binding CurrentTaskTitle}" FontSize="14"
                       Foreground="{DynamicResource SidebarForeground}"
                       HorizontalAlignment="Center" Margin="0,0,0,6"/>
            <TextBlock FontSize="14" Foreground="{DynamicResource SidebarForeground}"
                       HorizontalAlignment="Center" Margin="0,0,0,20">
                <Run Text="Помидор "/>
                <Run Text="{Binding PomodoroCount, Mode=OneWay}"/>
                <Run Text=" из "/>
                <Run Text="{Binding PomodoroTarget, Mode=OneWay}"/>
            </TextBlock>

            <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,20" MaxWidth="280">
                <ItemsControl ItemsSource="{Binding TodayTomatoes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="&#x1F345;" FontSize="22" Margin="0,0,4,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>

            <WrapPanel HorizontalAlignment="Center">
                <Button Content="&#x25B6; Старт" Click="Start_Click" Padding="14,8" Margin="0,0,6,6"
                        Background="{DynamicResource AccentColor}" Foreground="{DynamicResource AccentForeground}"
                        FontSize="13" FontWeight="SemiBold"
                        Visibility="{Binding CanStart, Converter={StaticResource BoolToVisibility}}"/>
                <Button Content="&#x25B6; Продолжить" Click="Start_Click" Padding="14,8" Margin="0,0,6,6"
                        Background="{DynamicResource AccentColor}" Foreground="{DynamicResource AccentForeground}"
                        FontSize="13" FontWeight="SemiBold"
                        Visibility="{Binding IsPaused, Converter={StaticResource BoolToVisibility}}"/>
                <Button Content="&#x23F8; Пауза" Click="Pause_Click" Padding="14,8" Margin="0,0,6,6"
                        Background="{DynamicResource WarningColor}" Foreground="White"
                        FontSize="13" FontWeight="SemiBold"
                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}"/>
                <Button Content="&#x23F9; Стоп" Click="Stop_Click" Padding="14,8" Margin="0,0,6,6"
                        Background="{DynamicResource DangerColor}" Foreground="White" FontSize="13"/>
                <Button Content="&#x23ED; Пропустить" Click="Skip_Click" Padding="14,8" Margin="0,0,6,6"
                        Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ButtonForeground}" FontSize="13"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
